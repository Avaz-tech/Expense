# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# UX IMPROVEMENTS: Date Filtering

1. **Month Selection in Stats**: Users should be able to toggle between different months, not just the current one.
2. **History Filtering**: The history view should allow filtering by a specific date or date range.
3. **Flexible Stats**: The calculation logic should support custom date ranges.
